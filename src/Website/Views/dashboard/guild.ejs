<!DOCTYPE html>
<html>
<head>
	<title>Tsukari Dashboard - Guild</title>
    <%- include("./templates/head") %>
    <link rel="stylesheet" href="/stylesheets/dashboard.css">
    <link rel="stylesheet" href="/stylesheets/grid.css">
</head>
<body style="background-color: var(--bs-gray-400);">
    <%- include("./templates/navbar",{li:[{href:"../../",name:"home"},{href:"./prefix",name:"prefix"}]}) %>

    <section class="text-center content-section" style="background: var(--bs-gray-400);">
        <div class="container">
            <h1 class="text-dark"><%= guild.name %></h1>
            <div class="grid">
                <div class="card shadow border-start-warning py-2">
                    <div class="card-body">
                        <div class="container align-items-center text-center no-gutters" >
                            <div class="row">
                                <div class="col-md-12">
                                    <div class="text-uppercase fw-bold text-xs mb-1">
                                        <a style="font-family: var(--bs-body-font-family); font-size: 75px;" href="./prefix" class="text-dark">Prefix</a>
                                    </div>
                                    <div div class="text-uppercase fw-bold text-xs mb-1">
                                        <span style="font-family: var(--bs-body-font-family); font-size: 15px;" class="text-dark">Current Prefix = [<%= dbguild.prefix.join(",") %>]</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <%- include("./templates/scripts") %>
</body>
</html>